eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    serviceUrl:
      defaultZone: http://localhost:${server.port}
server:
  port: 8998
spring:
  application:
    name: eureka-server
  security:
    basic:
      enabled: true
    user:
      name: kishan
      password: password